#include "matematica.h"
#include <math.h>

float seno(float angulo) {
    return sin(angulo);
}

float cosseno(float angulo) {
    return cos(angulo);
}

float tangente(float angulo) {
    return tan(angulo);
}
